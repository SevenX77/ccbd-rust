# d1-codex 任务:db::system.rs Wave-2 候选1 低风险子集 —— 边界设计稿

## 背景(自包含,新会话无前情)

项目 `ccbd-rust`(ah = Agent Hypervisor)正在做代码库模块化解耦(MD2 系列)。上一位 master 已写好 survey:
`.kiro/specs/ah-modular-decoupling/wave2-plan-draft-2026-07-13.md`(先读这份,里面"候选1"章节就是你要执笔的这块)。

`src/db/system.rs`(现 3491 行,HEAD 87bd01d)混了四类职责:
1. **启动 reconcile**:`reconcile_startup*` 系列(:1263-1520 一带,含 `reconcile_startup_with_tmux_socket_and_gateway`、`reconcile_claude_gateway_seats`、`select_startup_claude_gateway_seats_sync` 等)+ `reconcile_active_agents_to_crashed_sync`(:852)。
2. **system dump**:`system_dump`(:1220,async 包装)+ `system_dump_sync`(:100,`pub(crate)`)。
3. **socket/沙盒目录清扫**:`sweep_stale_tmux_sockets_sync`(:1521)、`remove_agent_sandbox_dir_sync`(:940)、`remove_agent_sandbox_dir_preserving_home_sync`(:961)。
4. **级联清理**(⚠️ 本轮不动,见下)。

## 你的任务范围(硬边界,不许扩大)

只设计 1/2/3 三类的拆分边界,**绝不碰第 4 类**:
- `cascade_kill_session_agents_sync`(:188)、`cascade_kill_session_agents_with_runner_sync`(:412)、`cascade_kill_session_agents`(:1224)、`cascade_kill_session_agents_for_daemon`(:1235)、`clean_worker_runtime_resources_sync`(:274/:393)、`snapshot_master_death_session_activity`(:216)、`stop_session_anchor_for_session_sync`(:552)、`session_agent_ids_sync`(:556)/`session_agent_ids`(:1256)。
- 原因:`cascade_kill_session_agents_sync` 是 target3(master_watch reaper)设计稿点名的"master 自愈 saga 耦合点",物理住在 db 层但语义上是 master 自愈的一部分,按 operator 铁律("触及 master 自愈 = 升级到 operator gate")不进本轮自闭环批次。你的设计稿必须显式声明"第 4 类保持原地不动、不重命名、不改可见性",给后续实施者和 r1 一个硬红线。

## 交付物

写一份边界设计稿到 `.kiro/specs/ah-modular-decoupling/d1-system-split-design-2026-07-13.md`,内容至少包含:

1. **子模块切法**:建议把 1/2/3 三类各自搬进 `src/db/system/startup.rs`、`src/db/system/dump.rs`、`src/db/system/sweep.rs`(或你认为更好的命名/结构,给出理由),`src/db/system.rs` 保留为门面,`pub use` re-export 现有公开符号,**不改变任何函数签名、可见性、调用点**——这是纯搬移,不是重构。
2. **依赖检查**:1/2/3 三类之间以及与第4类之间如果有直接函数调用(不只是"同文件"),必须列出来,并说明搬完之后怎么互相 `use`(避免循环依赖)。
3. **测试搬迁**:文件里 1574 行之后是测试(:1881 起等),说明这些测试各自归属 1/2/3 里的哪一个,搬去对应子模块的 `#[cfg(test)] mod tests`。
4. **零回归论证**:参照 target3 设计稿的论证结构(读 `.kiro/specs/ah-modular-decoupling/` 下 target3 相关设计稿作为范例),说明这次纯搬移为什么行为不变——例如全部 `pub(crate)`/`pub` 符号路径不变(靠门面 re-export)、没有新增/删除任何逻辑分支。
5. **给实施者的验收锚点**:钉死至少一条"新增的行为保持"测试名+断言目标(例如验证门面 re-export 后旧调用路径 `crate::db::system::sweep_stale_tmux_sockets_sync` 仍可用,或验证子模块间无循环 import 导致的编译问题)。

## 约束

- 你只执笔设计稿(markdown),不动 `.rs` 代码。
- 完成后把设计稿 commit 到 main(`git add` 该文件 + commit),不要留在工作区未提交——实施者稍后会开 worktree,worktree 只拿已提交状态。
- 设计稿写完直接结束,不要自己派其它 agent、不要开始实施。
