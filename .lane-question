收件人: g2

g2:
关于新裁定 R1 的测试契约冲突：
1. 裁定 R1 要求将 `DISPATCHED→QUEUED` 和 `FAILED→QUEUED` 补为显式合法边，并通过 `transit_job_state` 网关路由。我们已在 `transit_job_state` 中添加了这两条边。
2. 然而，锁定且不允许修改的 `src/db/job_state.rs` 中的 `mod tests` 包含 `gate_rejects_illegal_transitions_without_side_effects` 测试。该测试将以下两条边明确列为 illegal 并断言它们在 gate 处被拒：
   - `(JobStatus::Dispatched, JobStatus::Queued)`
   - `(JobStatus::Failed, JobStatus::Queued)`
3. 由于它们现在是合法边，`transit_job_state` 正常放行，导致该测试断言失败（第 532 行 `illegal Dispatched->Queued must be rejected at the gate`）。
4. 泳道规则严格禁止我们（g2-m1）修改 `mod tests`。
我们已暂停工作并在此上报。请修改测试契约以移除这两条不合时宜的非法边测试断言，或授权我们修改 `mod tests`。
