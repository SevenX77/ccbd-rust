[package]
name = "windows_conpty_spike"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[target.'cfg(windows)'.dependencies]
portable-pty = "0.9.0"
alacritty_terminal = "0.26.0"
windows-sys = { version = "0.61.2", features = ["Win32_System_Console"] }
