# o1 发散/红队单 · 编排底座第一性重构(轨2,2026-07-11)

## 你的角色
你是 o1(设计辩论席,只发散/红队/推翻假设,不执笔)。本单产出是问题域铺开 + 候选架构 + 红队备忘,**不是冻结设计**——冻结稿由 d1 执笔,且轨2 设计收敛后必须先经 operator 带用户过目才能放行实施(你我都无权自放行)。

## 最高原则(CEO 定,写死不可违)
第一性原理 · **不打补丁**(同族病≥2 次=结构病,升维重设计,不允许对下列症状逐条打补丁式修复) · 模块化低耦合高内聚 · **不要后兼容**(可推倒重写,不背历史包袱)。

## 输入语料(必读)
`research/orchestration-substrate-redesign/CORPUS-lane-problems-2026-07-11.md` —— 汇编了:
- 公开仓 ah#16/17/19/20/21/22/23/24(控制面一致性、感知/派单就绪判定、生命周期看门狗、可观测性、感知/恢复、master 自驱、存储卫生、生命周期/teardown)
- dev 仓 dev#139/140
- 内部 dogfood obs#47-52(规则热更、止血误伤、cancel/respawn 竞态三连、冻结引用文档 untracked、泳道死锁三重奏、僵尸 job 排水误派)
- `.kiro/specs/ah-orchestration-reliability/` 全目录(已立案根因深描)
- A/B 实验活体证据(`REVIEW-gateway-ab-verdict.md` 附录庚:泳道臂 14 轮返工 CI 从未绿 DNF,单干臂自主达绿)
- 北极星文档(`research/perception-layer-first-principles.md`、`research/perception-final-convergence-2026-07-09.md`)——**你的设计必须回答这两份文档提出的四题**:①单写入口硬约束形态;②各信号类 Unknown 预算;③cgroup 委托布局 PoC;④hook 归属竞态。

汇编者(operator)的判断,你的任务是**推翻或重立它,不是确认它**:上述症状不是 N 个独立 bug,而是一个编排底座在"感知 / 完成协议 / 控制面状态机 / 生命周期"四个关节上反复漏的同一批结构缺陷——单写仲裁缺失、unknown 造状态、高可靠信号缺席时静默回退 pane 推断、cancel/dispatch/respawn 路径非原子竞态、完成信号无显式协议。

## 需要你发散/红队的方向

1. **这个"同一结构病"判断本身站得住吗?** 逐条把 A-E 症状按根因分类,检查是否真收敛到感知/完成协议/控制面状态机/生命周期这四个关节,还是其实有第五类被漏掉(例如"人机协作边界"——obs#22 master 唤醒文本打进 composer 不提交,是感知问题还是交互协议问题?);或者反过来,四类里有没有实际是同一个更底层原语缺失的表象(例如"没有原子事务边界"能同时解释 ah#16 realign 非原子 + obs#49 cancel/respawn 竞态 + obs#52 排水误派)。

2. **候选架构铺开**:至少提出 2-3 个第一性候选(不是修补现状),例如:
   - 状态机为中心:所有 agent/job 状态变更走单一事务化状态机(类似 CRDT/事件溯源),消灭"job 说 COMPLETED 但产物轨说没有"的分裂真相源。
   - 信号分级协议:显式区分"高可靠信号"(hook/DB 事务)vs"启发式信号"(pane 内容推断),任何启发式信号必须标注置信度且有 Unknown 预算上限,而非静默当真。
   - 完成协议显式化:引入类似 two-phase commit 或显式 ACK 协议的"任务完成"握手,而不是当前"job 状态 + pane 推断"的双轨且互相不校验。
   红队每个候选:它解决了 A-E 里的哪些症状?它引入了什么新故障模式?对现有 ah/ahd 架构的爆炸半径有多大?

3. **感知层北极星四题**,逐题给出你的初步立场(可以是"我认为现状的答案是错的,应该是 X"这种推翻式立场):
   - 单写入口硬约束的形态应该长什么样(谁是唯一权威写者,如何强制)?
   - 各信号类(hook/log/pane-scrape/DB)的 Unknown 预算怎么定(超预算就该做什么——报警?降级为纯观察模式?阻止派单?)
   - cgroup 委托布局的 PoC 方向(如果与生命周期/资源隔离相关,提出可验证的最小实验)。
   - hook 归属竞态(多 agent/多 hook 触发时如何判定谁的信号该采信)的解法方向。

4. **不打补丁红队**:对每个你考虑纳入的修复点,检查它是否只是把 A-E 里某一条症状的表面现象堵住(打补丁),还是真的消除了结构病根因。如果拿不准,标注"存疑,需要 d1/operator 决定要不要收进这版重构范围"。

5. **爆炸半径分级**:标注哪些改动是"地基级"(动了就要求几乎所有实施位配合迁移,如状态机换代)、哪些是"局部级"(可独立模块替换)。这个分级会直接影响 operator 带用户过目时的决策难度,请显式标注,不要藏在长文里。

## 交付形式
- 一份发散/红队备忘,覆盖上述 1-5 点。
- 明确区分"我认为应该推翻现状判断"部分 vs "我认为现状判断成立、只是需要更底层方案"部分。
- 不要写 tasks.md、不要写验收测试代码、不要写"最终架构设计"格式的文档——那是 d1 的执笔权限。
- 完成后写入 `research/orchestration-substrate-redesign/o1-divergence-memo-2026-07-11.md` 并在会话里告知完成。
