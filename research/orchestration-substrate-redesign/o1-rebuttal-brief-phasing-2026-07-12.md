# o1 反驳单(窄口径)· 分阶段灰度 vs Big-Bang 推倒重写(轨2,2026-07-12)

## 背景与仲裁(用户/operator 已裁决的部分,不要重开)
用户看过你的原始发散备忘(`o1-divergence-memo-2026-07-11.md`)与 d1 的收敛草稿(`design-substrate-redesign-draft-2026-07-12.md`)后,operator 核对逐条分歧,裁决如下:

**已按 d1 收敛、不再辩的机制/实证类分歧**(不要在本单重提):
- 2PC/原子分布式事务在 tmux/内核/SQLite 非事务性资源上不可能——d1 驳回成立。
- epoch 用单调版本戳(复用现存 `state_version`)而非可续租约——d1 收敛成立。
- GC(垃圾回收)是 Reconciler 脊柱内在收敛 pass,不是可独立的 local 外挂模块——operator 核对你原文 §四.2 后发现**你自己的红队本就落到 Active-Reconcile + 物理 GC 这个结论**,只是你在 §五 爆炸半径分级里把 GC 标签贴成了"local",与你自己 §四.2 的实质结论矛盾。这是你的标签错位,不是 d1 曲解你,判 d1 对。

**唯一必须补辩的一条(你的强项,本单只辩这个)**:
d1 收敛稿 §六(139 行)写:"本重构**以地基级为主,是底座推倒重写,不是补丁集**",并在 §九(216 行)自陈:"实施排期/切换策略(如何从现状灰度到新底座)本稿未展开"。

operator 判读:**你在原文 §五的"爆炸半径分级"本意是"决策参考/可分阶段实施"**("为提供清晰的决策参考"),d1 把它读成了"四项地基级变更高度耦合→必须一次性 big-bang 推倒重写",这是对你原意的稻草人式简化——你从未主张过必须 big-bang,d1 也承认自己没展开迁移路径。这个空白需要被真正填上,不是靠重申"高耦合"带过。

## 你的任务(窄口径,严格限定范围)
**只回答一个问题**:编排底座四项地基级变更(GF1 单 Reconciler+事件日志脊柱、GF2 epoch 全物理资源绑定、GF3 slot 工作区物理隔离、GF4 显式完成协议+控制面 API 化)**能否分阶段灰度上线,而不是必须一次性整体切换**?

**不要重辩**已经用户/operator 裁决过的三条(2PC、epoch 形态、GC 归属)——那些已定,再辩视为违反窄口径纪律。

请给出你能想到的**最强**分阶段/灰度路径设计,至少覆盖:
1. **切入顺序**:四项地基级变更里,哪一项可以先独立上线并在生产验证,而不必等其它三项就绪?(例如:GF1 的 Reconciler 是否可以先以"影子模式"跑——只读现状、产出事件日志、不接管写权限,与现有 `pub(crate) mark_agent_*_sync` 并存一段时间,通过对比 Reconciler 的判定与现状实际行为来验证正确性,验证够了再切实际写权限?)
2. **共存期的双写/双轨风险**:如果确实要并存一段时间(旧的 `mark_agent_*_sync` 直接写 + 新 Reconciler 影子跑),会不会引入新的不一致?怎么设计共存期的安全边界(例如:影子模式下 Reconciler 绝不写,只读+告警,把双写风险降为零)?
3. **GF3(slot 工作区隔离)与其它三项的真实耦合度**:d1 说"四项彼此高度耦合共享 Reconciler 脊柱",但 GF3(工作区隔离)是否其实可以独立于 GF1/GF2/GF4 先上——它动的是 spawn 时的 cwd 装配,不动状态机写权限,你觉得这条能不能第一批独立灰度、降低第一刀的爆炸半径?
4. **失败回退路径**:分阶段上线的每一步,如果验证失败,回退路径是什么(纯配置开关关掉 Reconciler 影子模式 vs 需要回滚代码)?

## 边界
- 严格只辩"能否分阶段/怎么分阶段"这一个问题,不要滑到重新论证已裁决的机制点。
- 你的产出是给 d1 用来更新冻结稿的**输入**,不是你自己去改 d1 的稿子或写最终方案——你只提供"最强分阶段路径提案"这一份反方材料。
- 不写代码、不写 tasks.md、不碰 git。
- 完成后写入 `research/orchestration-substrate-redesign/o1-rebuttal-phasing-2026-07-12.md`,在会话里回报一句话摘要(能不能分阶段+你认为最优先上线哪一项)。
