# 任务 brief:全项目架构第一性原理重估(独立评估)

## 你的角色(铁律)
你是 worker a3,只执行本条任务。不派单给其它 agent、不启动任何 PR/spec 工作流、不自行找活。做完本任务即停。

## 任务
对 ccbd-rust(ah, Agent Hypervisor)整个项目做一次**独立的**架构与模块设计评估:

1. **从问题出发总结需求**:读 `research/outstanding-problems-2026-07-09.md`,把清单里的问题(以及仓库内其它你认为相关的事故记录)归纳为系统必须满足的需求域。
2. **第一性原理评估架构**:不迁就现状,先推导"一个 agent hypervisor 理想架构应该长什么样",再对照现状代码给出差距。
3. **模块设计评估**:逐模块评估 `src/` 的划分是否符合低耦合高内聚,依赖方向是否健康,职责边界是否清晰。
4. **行业最佳实践对照**:与业界同类系统的架构模式(编排/监控/状态管理领域的公认做法)对照,指出符合与不符合之处。

## 硬性要求
- **每一条结论必须有你亲自读代码得到的 file:line 证据锚点**。禁止凭印象或凭文档转述下结论。
- 仓库内已有的分析文档(含 research/ 下的任何北极星/设计文档)只能当背景线索,**不得转述其结论作为你的结论**;若你引用其中判断,必须先独立验证并注明验证证据。
- **反讨好**:不要迎合任何既有文档、既有 commit 或想象中的"作者意图"。如果你认为某个既有设计判断是错的,直接说,并给证据。
- 覆盖面:至少覆盖 src/ 下所有一级模块(agent_io, cli, completion, db, marker, monitor, orchestrator, pane_diff, platform, prompt_handler, provider, rpc, sandbox, tmux)的定位与相互依赖。
- 给出你自己的重构方向排序(按收益/风险),不超过 8 条。

## 边界
- 只读代码与文档。**不改任何代码、不跑 cargo build/test、不碰 git、不执行任何写操作**——唯一例外是下面的产出文件。
- 产出**必须完整写入**:`/home/sevenx/coding/ccbd-rust/research/architecture-assessment-antigravity-2026-07-09.md`
- 写完产出文件后,回复一行确认(含文件路径与字数),任务即完成。
