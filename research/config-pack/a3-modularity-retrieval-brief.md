你是 worker(explore/深度调研角色)。继续深挖,**这轮不是找解法、不是写包,是把问题挖到最底层**。用户明确说"还没挖到通透,还在表层",所以你的任务是**把根挖深、把设计空间摊开,别急着给方案或收敛**。

【一个正在验证的"最底层根"假设(请你严肃地帮忙证实或证伪它)】
> AI agent(乃至人类协作者)开发时**只有"局部视野"**:每个任务只看得见眼前的文件/组件,看不见系统的架构、契约、权威源在哪。看不见全局 → 只能局部优化(局部是它唯一能廉价看到的)。由此派生出:打补丁不深究共享底座、把代码堆进大文件不做模块化、单个功能不管全局性能、每次都从头搜设计文档+代码库。
请你在下面三个调研里,处处回扣这个根:这些现象是不是都能归到"缺乏廉价的全局态势感知"?有没有反例?

【调研一:大文件普查(具体数字,用户点名要)】
扫 agent-harness 仓(`packages/graph-agent`、`packages/graph-agent-gateway`、`apps/studio/backend`、`apps/studio/frontend/src`):
- 各语言(.py / .ts / .tsx)文件行数分布:>400 行、>800 行、>1500 行各多少个?
- 点名**最大的 10 个文件**(路径 + 行数)。
- 挑最大的 2-3 个,一句话说它把多少个本应分开的职责堆在了一起(粗看即可)。
- 回扣根:大文件是不是"局部堆积、没有模块地图"的症状?

【调研二:模块化解耦纪律(为什么 AI 天然不做 + 业界怎么治)】
- **根behavior**:为什么 LLM 天然偏向把代码堆进一个大文件、不主动拆模块解耦?(上下文局部性?生成惯性?没有"该拆了"的信号?)
- **业界成名纪律**:单一职责、文件/函数行数上限、模块边界(依赖方向)、关注点分离——点名具体规矩和**可机器检查的门禁**(如 max-lines lint、圈复杂度、import 依赖边界检查工具 eslint-plugin-boundaries / import-linter 之类),大白话解释。
- **难点**:"拆到什么粒度算对"能不能机械判定?哪部分是死规则(可门禁)、哪部分是判断(要脑子)?

【调研三:检索知识库 + 文档互联(最深的一条,重点挖)】
用户原话:"每个项目本身应该有一个让 ai 能够快速检索、快速定位的知识库,文档和文档之间的接口要打通,互相串联"。今天我们自己找 compile 真中枢都从前端表面搜、搜歪了,就是活证。
- **问题本质**:一个 agent 接到"改 X 功能 / 修 Y 问题",怎么**快速定位到所有相关信息**(设计文档 + 代码 + 契约 + 权威源在哪层),而不是每次从头 grep 全库?
- **业界/工具的做法有哪些**(点名并大白话解释):代码地图 / 符号索引(Sourcegraph、ctags、LSP)、ADR 架构决策记录、doc-as-code、仓库级 index/orientation 文件、依赖图、RAG/embedding 检索、"AGENTS.md/CLAUDE.md 分层入口"、跨文档超链接锚点……各自解决检索的哪一面、代价多大?
- **文档互联具体长什么样**:文档↔文档的"接口"、文档↔代码的锚点(file:line / 符号),怎么做到改了代码不让文档腐烂?
- **轻重之分**:哪种是**轻量、每项目能自持**的(适合 master+workers + 一个 `.ah/` 知识库);哪种是**重型、要专门系统**的(scs 级,如全库 embedding + 语义索引服务)?
- 回扣根:这个检索知识库是不是"制造全局态势感知"的核心解药?它能覆盖根的多大比例?剩下的靠什么(门禁?强模型?)?

【产出】写 `research/config-pack/a3-modularity-retrieval-research.md`,三节 + 每节回扣根假设 + 最后一句总判:"局部视野"这个根到底成不成立、检索知识库能解多少。完成回一句话:大文件最大几行/几个超标 + 根假设成不成立 + 已落盘。

【约束】只读+写这一个文件;不写包、不改代码、不碰 git。这轮的价值是**挖深、证伪、摊开设计空间**,不是给方案。大文件普查要给真实数字(你能读 agent-harness 仓的话就实扫;读不到就明说读不到、别编数字)。
