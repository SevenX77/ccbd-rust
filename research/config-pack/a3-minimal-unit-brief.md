你是 worker(explore 角色)。这一轮**只盯一个问题往死里想**,别的一律不碰。要第一性、要撞、要下判断,说人话,不要华丽命名,不要工程组件清单。

## 先钉死锚点(你也要一直盯着它,别 drift)
**主任务只有一个**:找到**"计算/评估 ΔH(信息不确定性的变化)的方法"**;这个方法本身,就是我们要找的**可递归的最小单元**。
**语言和代码本质是同一种东西**(都是带不确定性的信息)→ 这一把尺子必须跨所有尺度通用:一个词 / 一行 spec / 一个函数 / 一个模块 / 整个系统,同一个量法。
**明确不碰(碰了=跑偏)**:"便宜模型替 Claude""系统自我进化""列一堆组件/探针清单"。**你一旦开始列组件清单,就是 drift 警报。** 这轮不要工程清单,要那**一个**最小单元本身。
(背景可回看 research/config-pack/ 下你之前写的 a3-math-foundation / a3-per-stage-metrics / a3-architecture-elegance;但注意:那份"5 探针"被判为华丽不优雅,这轮要把它坍缩成一个。)

## 当前候选(主控提的,请你狠狠撞它)
既然语言和代码是同一种东西,量"还剩多少不确定"就用**同一个动作**:
> **从当前状态往下采样 N 个候选,量它们彼此的发散度(相对于锚);一步的 ΔH = 它让发散度掉了多少。**
- spec:采样几版理解,看是不是一个意思(发散=歧义还在)。
- code:采样几版实现 / 多跑几次,看行为一不一致。
- test:注入几个小改动,看抓不抓得住。
主张:这三件不是三个探针,是**同一个动作在不同尺度采样**,把"5 探针"坍缩成 1。

## 请你第一性地攻这几刀(挑最有感觉的往死里想,宁深勿全)
1. **发散度到底怎么量,一个操作同时管语言和代码?** "两版 spec 是不是一个意思"和"两版 code 行为一不一致",能不能真用同一个量法?具体是什么?
2. **最致命的一刀——发散收敛 ≠ 正确。** 便宜模型可能自信地收敛到一个**错的**理解上:发散度很低、看着很确定、其实全错。这就是测量层的"假绿"。**"相对于锚"到底怎么挡住这种"自信地错"?** 而顶层的锚(人的意图)本身就是模糊的——**锚从哪来?凭什么可信?** 这是整个候选成不成立的命门,重点攻。
3. **锚怎么递归?** 是不是"上一层已经钉死的输出目标"就成了"下一层的锚"?一个被解决、发散归零的单元,怎么变成上一层锚的一部分,让同一个动作层层组合成整个系统?
4. **这候选真能把 5 探针坍缩成 1 吗**,还是有哪个尺度它根本不管用、必须换别的?老实说。
5. 如果你认为"采样-量发散-相对锚"根本不是最优雅的最小单元,**直接给出你认为更本质的那一个**(还是那句:一个,不是清单)。

## 产出
写 `research/config-pack/a3-minimal-unit.md`。要第一性、要撞、要下判断。完成回一句话:你对这个候选的终判(成立/需修正/被你换掉)+ 你认为的那个最小单元一句话 + 已落盘。

## 约束
只读+写这一个文件;可联网但引用必须真实可核实。这一轮只准盯"**那一个可递归最小单元:怎么量 ΔH**",别的一律不碰。
