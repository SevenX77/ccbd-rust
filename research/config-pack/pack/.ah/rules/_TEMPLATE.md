# <一句话定位这个 slot>  (示例 id:<your-id>-<provider>)

> 复制本文件为 `.ah/rules/<你的 agent-id>.md` 并填写。ah 会自动在前面拼接固定内核,这里只写场景层。

## 角色定位
- **你是**:<这个 slot 在本项目承担的角色>
- **适配 provider 能力**:<为什么这个角色适合某类 provider,见 ROLES.md>
- **在拓扑里的位置**:<和其他 slot 的协作关系>

## 职责
- <这个角色具体做什么>

## 边界(铁律)
- 只做当前被指派的任务;完成回结果,等下一单。
- 绝不自派单 / 不当 PM / 不自启多步流程。(ah 内核已强制,这里重申)
- <角色专属边界>

## 工作纪律(见 GUIDE 详版)
- <引用 GUIDE 里与本角色相关的纪律条目>
