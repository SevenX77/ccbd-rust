你是 worker(explore/深度调研+设计角色)。这一轮是**独立攻一个"大问题"**,不是写包、不是 review。主控(我)会同时独立出一版,回头我们两版**对着找茬、互相启发**——所以**别锚定我的观点,给你自己的独立判断**。

【必读料(按序)】
1. `research/config-pack/user-reframe-2026-07-06.md` —— 用户 2026-07-06 亲口的目标澄清+六条更正,**第一手真相,以此为准**。
2. `research/config-pack/reference/agent-harness-AGENTS.md` + `research/config-pack/reference/agent-harness-frontend-single-agent-SOP.md` —— 用户真实项目 agent-harness 的两份实战 SOP(后者明说"替代重型多-agent PM 工作流"的单-agent SOP)。这是"打过仗"的一手样本。
3. 你自己写的 `research/config-pack/dev-journey-research.md` + `a3-design-questions.md`。

【背景一句话】用户是**产品经理视角、非资深工程师**,能给产品意图、给不了架构判断。他用多-agent SOP 开发 agent-harness,踩的坑不是 spec 差(spec 是黄金),是:①功能不按前端用户线索逐个实现、没 e2e、没亲自逐个验收;②**设计层缺了架构层的工程设计(用户的经验盲区),而没有任何人/机制被指派去从顶层补齐**;③AI 惯性打补丁(a 坏改 a,不深究 a、b 共用的 c)。

【本轮的大问题(独立攻它,这是核心)】
**谁/什么来填补"用户资深架构工程师经验的缺口"?** 具体说:谁站着盯**跨功能**的——事件订阅模式、数据规范(schema)、API 规范(契约)、模块解耦是否充分——保证它们跨多个功能保持一致、不漂移、不重复造轮子?
- 这是一个 **per-feature 审阅者解决不了的、站着的跨切面职责**。它得在"每个功能动手前"就把工程架构立起来,还要在"多个功能之间"防漂移。
- 用户明确怀疑:**这可能是 spec-coding-system 级的大设计,不是 master+workers 小组能解的**。请你**严肃对待这个怀疑**——认真判断:这个"架构良心"哪一部分**天然就是 scs 级**(master+workers 结构上做不到)、哪一部分**master+workers 能承接**?
- 关键难点:用户**填不了这份架构契约(盲区)**。所以这个角色不能只是"审阅已有设计",它得**主动替用户把架构提出来**——从产品 spec+代码反推工程架构、把用户不知道该问的跨切面决策摆到他面前、只让他拍板产品后果。这个"提出用户提不出的架构"到底能不能自动化?难在哪?

【要求】
- **独立给你的判断**,别写"选项 A/B"式的骑墙罗列——这轮要你敢下判断、敢说"我认为 X 是 scs 级做不到、Y 能落在 master+workers"。
- **主动找茬那个最naive的答案**:"就加一个 architect-reviewer agent 站着审不就行了?"——把它的漏洞捅穿(它为什么填不了盲区?为什么防不住跨功能漂移?)。
- **把大框架和小组的关系摆清**:先讲大框架(架构良心该由什么承载:角色?活的工程契约文档?硬门禁?三者组合?),再讲 **master+workers 工作小组在这个大框架里摆在哪个位置**、它的边界到哪、它够不到的部分留给谁(scs / 用户 / 未来机制)。
- **次要、别喧宾夺主**:顺带点一下用户 #6——现在只靠 CLAUDE.md+memory 很脆易失焦,哪些该沉淀成 skill、哪些要 hooks 保证可靠、哪些要 mcp server、项目工程规范怎么守。但这是"小组层"的实现手段,**放在大框架结论之后**,别喧宾夺主。

【产出】写 `research/config-pack/a3-big-question-take.md`,结构:①我对大问题的独立判断(架构良心由什么承载)②哪部分是 scs 级 / 哪部分 master+workers 能承接(敢切)③捅穿 naive 答案的漏洞 ④小组在大框架里的位置+边界 ⑤(次要)skill/hooks/mcp/规范守护 的初步映射。完成回一句话:核心判断一句话概括 + 已落盘。

【约束】只读+写这一个输出文件;不写包、不改代码、不碰 git。重点是敢下独立判断、把大问题攻透,给主控一版能对着找茬的东西。
